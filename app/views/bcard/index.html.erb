<h1>Bcard#index</h1>
<p>Find me in app/views/bcard/index.html.erb</p>
<%= link_to 'Add-Card', '/bcard/new', class: 'link-text' %>

<!-- <% if @ddlValue %>
<%= @ddlValue.company_name %>
<% end %> -->

<!-- <%= form_with url: "/bcard/index", method: "GET" do |ddl| %>
    <div class="detail-title">group by</div>
    <%= ddl.collection_select(:company_name, @ddlBCards, :company_name, :company_name ) %>
    <%= ddl.submit "grouping" %>
<% end %> -->

<%= form_tag "/bcard/index", method: "GET" do%>
    <div class="select">
        <select name="company_name" id="ddl">
            <% @ddlBCards.each do |bcard| %>
            <option value="<%= bcard.company_name %>"><%=bcard.company_name %></option>
            <% end %>
            <% if @current_user.authority == 99 %>
            <option value="all">all</option>
            <% end %>
            <!-- afterbegin -->
            <option value="default"  id="prompt" selected="selected">SortByCompany</option>
            <!-- beforeend-->
        </select>
        <input type="submit" value="Search" class="link-text">
    </div>
    
<% end %>

<!-- class="display-center" -->
<!-- テーブルでの表記はしない -->
<!-- <div> 
    <table class="bcards">
        <tr>
            <th class="th-company">Company</th>
            <th class="th-name">Customer</th>
            <th class="th-phone_num">Phone</th>
        </tr>
        
    <% @BCards.each do |bcard| %>
        <tr>
            <td class="td-company"><%= bcard.company_name %></td>
            <td class="td-name"><%= bcard.name %></td>
            <td class="td-phone_num"><%= bcard.phone_num %></td>
            <td><%= bcard.user_id %></td>
            <td><%= link_to 'edit', "/bcard/edit/#{bcard.id}", class: 'link-text' %></td>
            
        </tr>
    <% end %>
    </table>
</div> -->

<!-- デザイン -->

<!-- コンテナを中央寄せにするだけ -->
<div class="display-center">

    <!-- コンテナ。bcard単位の入れ物で、max-width,min-widthを作る -->
    <div class="bcard-container">
        <% @BCards.each do |bcard| %>

            <!-- bcardをいれる。個々の名前や会社名などの親要素 -->
            <div class="bcard">
                <div class="bcard-company"><%= bcard.company_name %></div>
                <div class="bcard-name"><%= bcard.name %></div>
                <div class="bcard-phone">phone:<%= bcard.phone_num %></div>

            </div><!-- class="bcard" -->

        <% end %>
        
    </div><!-- class="bcard-container" -->

</div><!-- class="display-center" -->

